#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}

// to compile this code use command "gcc basic-syntex.c -o basic-syntex"
// to run this code use command "./basic-syntex"
// main function is the starting point of the program
// the main is called when the program is executed by the operating system or when the program is started automatically
// it is the first function that is called when the program is executed
// and it is the last function that is called before the program exits
// the return value of the main function is the exit status of the program
// it can create only one time in a program application
